# 1단계: React 빌드 (Builder 단계)
# node:18-alpine 버전의 이미지를 받아서 docker image를 생성 builder 이름으로 지정
FROM public.ecr.aws/docker/library/node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./

# 의존성 설치: 'npm ci'는 버전 잠금 파일(lockfile)을 엄격하게 따라서 설치하며,
# 보안 감사(audit) 등을 생략하여 설치 속도를 높입니다.
RUN npm ci --prefer-offline --no-audit

COPY . .

# 빌드 시점에 필요한 환경 변수(API 서버 주소 등)를 설정합니다.
# Docker build 명령을 내릴 때 외부에서 값을 주입받을 수 있습니다.
ARG REACT_APP_API_BASE_URL
ENV REACT_APP_API_BASE_URL=${REACT_APP_API_BASE_URL}

# React 앱을 빌드하여 실제 배포할 정적 파일(HTML, CSS, JS)을 생성합니다.
RUN npm run build

# 2단계: Nginx를 사용하여 웹 서버 실행 (실제 배포 단계)
# 빌드 도구가 없는 가벼운 Nginx 이미지를 사용하여 용량을 줄입니다.
FROM public.ecr.aws/docker/library/nginx:alpine

# 중요: 1단계(builder)에서 생성된 빌드 결과물(/app/build)만 가져와서
# Nginx가 웹 페이지를 보여줄 수 있는 기본 경로로 복사합니다.
COPY --from=builder /app/build /usr/share/nginx/html

# 사용자 정의 Nginx 설정 파일을 적용합니다.
# (React 라우팅 처리를 위해 보통 필요한 설정입니다.)
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 80번 포트를 열어 웹 접속을 허용합니다.
EXPOSE 80

# Nginx를 백그라운드가 아닌 포그라운드에서 실행하여 컨테이너가 꺼지지 않게 합니다.
CMD ["nginx", "-g", "daemon off;"]